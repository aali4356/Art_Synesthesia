---
phase: 03-text-analysis-input-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/input/QuickStart.tsx
  - src/components/input/index.ts
  - src/data/surprise-phrases.ts
  - src/app/page.tsx
autonomous: false
requirements:
  - UI-16
  - UI-17

must_haves:
  truths:
    - "Quick-start buttons below input offer 4-5 diverse text type suggestions"
    - "Clicking a quick-start button inserts text AND auto-triggers generation in one click"
    - "Surprise me button picks from a curated pool of ~50 phrases, different each click"
    - "Quick-start and Surprise me buttons are visible on the landing page below the input zone"
  artifacts:
    - path: "src/components/input/QuickStart.tsx"
      provides: "Quick-start buttons and Surprise me"
      exports: ["QuickStart"]
    - path: "src/data/surprise-phrases.ts"
      provides: "Curated pool of ~50 phrases"
      exports: ["SURPRISE_PHRASES"]
  key_links:
    - from: "src/components/input/QuickStart.tsx"
      to: "src/app/page.tsx"
      via: "onQuickStart callback inserts text and triggers generate"
      pattern: "onQuickStart|generate"
    - from: "src/components/input/QuickStart.tsx"
      to: "src/data/surprise-phrases.ts"
      via: "imports SURPRISE_PHRASES for random selection"
      pattern: "import.*SURPRISE_PHRASES"
---

<objective>
Add quick-start buttons and the "Surprise me" feature to the landing page, then verify the complete end-to-end flow with a visual checkpoint.

Purpose: Quick-start buttons provide instant gratification -- one click inserts text AND triggers generation. This is critical for first-time user experience: the user sees art immediately without having to think of what to type. The "Surprise me" button adds delight and replayability.

Output: Quick-start buttons and Surprise me on the landing page, wired to auto-trigger generation. Human-verified end-to-end flow.
</objective>

<execution_context>
@/Users/ahmadali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-text-analysis-input-ui/03-CONTEXT.md
@.planning/phases/03-text-analysis-input-ui/03-02-SUMMARY.md

<interfaces>
<!-- From plan 03-02 outputs -->

From src/app/page.tsx (after 03-02):
- 'use client' page with useTextAnalysis hook
- useState for inputText, isPrivate
- Conditional landing/results rendering
- InputZone component on landing view
- ResultsView component on results view

From src/components/input/InputZone.tsx:
```typescript
interface InputZoneProps {
  text: string;
  onTextChange: (text: string) => void;
  onGenerate: () => void;
  isPrivate: boolean;
  onTogglePrivate: () => void;
  isGenerating: boolean;
}
```

From src/hooks/useTextAnalysis.ts:
```typescript
export function useTextAnalysis(): {
  stage: PipelineStage;
  result: PipelineResult | null;
  generate: (text: string) => Promise<void>;
  reset: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quick-start buttons, Surprise me, and curated phrase pool</name>
  <files>
    src/data/surprise-phrases.ts,
    src/components/input/QuickStart.tsx,
    src/components/input/index.ts,
    src/app/page.tsx
  </files>
  <action>
    **Create curated phrase pool (src/data/surprise-phrases.ts):**
    - Export `SURPRISE_PHRASES: string[]` with ~50 diverse, evocative entries
    - Mix categories for variety:
      - Famous opening lines: "It was the best of times, it was the worst of times", "Call me Ishmael", "In a hole in the ground there lived a hobbit"
      - Evocative sentences: "The last sunlight turned the ocean into liquid gold", "She whispered secrets to the stars and they whispered back"
      - Fun facts: "Octopuses have three hearts and blue blood", "Honey never spoils -- archaeologists found edible honey in Egyptian tombs"
      - Recipes/instructions: "Fold the butter into the flour until it resembles coarse breadcrumbs", "Plant the seeds one inch deep, water gently, and wait"
      - Poetry fragments: "Do not go gentle into that good night", "I wandered lonely as a cloud"
      - Technical/code: "function fibonacci(n) { return n <= 1 ? n : fibonacci(n-1) + fibonacci(n-2) }", "SELECT name, age FROM users WHERE active = true ORDER BY created_at DESC"
      - Names/short: "Alexander Hamilton", "Tokyo at midnight", "The color blue"
      - Philosophical: "If a tree falls in a forest and no one is around to hear it, does it make a sound?"
    - Aim for diversity in length (short 2-word to long multi-sentence), tone (playful, serious, whimsical, technical), and text structure

    **Create QuickStart component (src/components/input/QuickStart.tsx):**
    - 'use client' component
    - Props: { onQuickStart: (text: string) => void; disabled?: boolean }
    - Define QUICK_START_ITEMS array of 5 buttons:
      1. { label: "your name", text: "" } — special: empty text means prompt user to type their name (just set placeholder focus)
         Actually, per user decision "Try: your name" should insert text. Use a generic name like the user's local time greeting: "Good morning" or similar. Better: just use a short evocative name like "Ada Lovelace" to demonstrate how names produce art.
         Simplest approach: "your name" button inserts "Ada Lovelace" as example text.
      2. { label: "a haiku", text: "An old silent pond\nA frog jumps into the pond\nSplash! Silence again" }
      3. { label: "a recipe", text: "Combine two cups of flour with a pinch of salt. Slowly add warm water, kneading until the dough is smooth and elastic. Let it rest for thirty minutes under a damp cloth." }
      4. { label: "a famous quote", text: "We are all in the gutter, but some of us are looking at the stars." }
      5. { label: "Surprise me", text: null } — special: picks random from SURPRISE_PHRASES
    - Layout: horizontal wrap of pill-shaped buttons below the input zone
    - Prefix text: "Try:" in text-muted font-light, then buttons inline
    - "Surprise me" has a distinct style: slightly different (e.g., border instead of bg, or a small sparkle/dice icon using unicode)
    - Clicking any button: call onQuickStart(text) — the parent handles setting text AND triggering generate
    - For "Surprise me": pick random phrase using Math.random() (non-deterministic is fine for UI randomness, not rendering)
    - disabled prop disables all buttons during generation (opacity-50, pointer-events-none)
    - Styling: text-sm, px-3 py-1.5, rounded-full, bg-[var(--muted)], hover:bg-[var(--border)], transition-colors, cursor-pointer
    - Playful, inviting tone per user decision

    **Update barrel export (src/components/input/index.ts):**
    - Add QuickStart to exports

    **Wire into page (src/app/page.tsx):**
    - Add QuickStart component below InputZone in the landing view
    - onQuickStart handler: receives text string, sets inputText state, AND calls generate(text) — pass the text directly to generate, NOT the stale state value (pitfall from RESEARCH.md)
    - For "your name" button: set inputText to the example name, generate with it
    - When isGenerating, pass disabled=true to QuickStart
  </action>
  <verify>
    <automated>npx next build && npx vitest run</automated>
  </verify>
  <done>
    - 5 quick-start buttons visible below input zone on landing page
    - Each button has a distinct text type (name, haiku, recipe, quote, surprise)
    - Clicking inserts text AND auto-triggers generation in one action
    - "Surprise me" picks a different phrase each click from ~50 curated entries
    - Buttons disabled during generation
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 3 end-to-end flow</name>
  <files>src/app/page.tsx</files>
  <action>
    Human verification checkpoint. No code changes — verify the complete Phase 3 flow visually.

    What was built:
    - Text analyzer with real NLP features (sentiment, syllables, entropy, etc.)
    - Input zone with tabs (Text active, URL/Data disabled)
    - Generate button with private mode toggle
    - Quick-start buttons and "Surprise me"
    - Staged pipeline progress indicator
    - Results view with collapsed input, placeholder canvas, and parameter panel
    - Landing-to-results page transition

    How to verify:
    1. Start dev server: `npx next dev`
    2. Visit http://localhost:3000
    3. Verify landing page: showcase placeholder, tagline, input zone with tabs, quick-start buttons below
    4. Type "Hello world" in textarea and click Generate
    5. Verify: progress indicator shows Parsing -> Analyzing -> Normalizing -> Rendering stages
    6. Verify: page transitions to results view with collapsed input bar at top
    7. Verify: placeholder canvas shows colored grid using generated palette
    8. Verify: parameter panel shows 15 bars with values and summaries
    9. Click "Edit and Regenerate" on collapsed input, modify text, regenerate
    10. Click a quick-start button (e.g., "a haiku") — verify it inserts text AND generates
    11. Click "Surprise me" multiple times — verify different text each time and auto-generation
    12. Verify URL and Data tabs are visible but disabled (dimmed, not clickable)
    13. Verify private mode toggle (lock icon) renders and toggles
    14. Resize browser to mobile width — verify stacked layout
    15. Test dark/light mode toggle — verify input zone and results look correct in both modes
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>User has verified the complete Phase 3 flow works end-to-end and typed "approved"</done>
</task>

</tasks>

<verification>
```bash
# Build succeeds
npx next build

# All tests pass
npx vitest run

# Manual verification via dev server (see checkpoint task)
```
</verification>

<success_criteria>
1. Quick-start buttons insert text and auto-trigger generation with one click
2. "Surprise me" draws from ~50 curated phrases, different each click
3. Complete end-to-end flow works: type text -> Generate -> progress -> results
4. Human-verified via checkpoint task
</success_criteria>

<output>
After completion, create `.planning/phases/03-text-analysis-input-ui/03-03-SUMMARY.md`
</output>
