---
phase: 01-foundation-determinism-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - vitest.config.mts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - DS-03

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "TypeScript compilation succeeds with strict mode"
    - "Vitest test runner executes and passes a sample test"
    - "ESLint runs without configuration errors"
    - "Geist Sans and Geist Mono fonts load in the browser"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      min_lines: 20
    - path: "vitest.config.mts"
      provides: "Vitest test runner configuration"
      min_lines: 10
    - path: "src/app/layout.tsx"
      provides: "Root layout with Geist fonts"
      min_lines: 15
  key_links:
    - from: "vitest.config.mts"
      to: "package.json scripts.test"
      via: "vitest command in test script"
    - from: "src/app/layout.tsx"
      to: "geist npm package"
      via: "GeistSans and GeistMono font imports"
---

<objective>
Scaffold the Next.js project with TypeScript, Tailwind CSS v4, ESLint, Vitest, and Geist fonts. Establish the folder structure that all subsequent plans build upon.

Purpose: Every other plan in Phase 1 (and all subsequent phases) depends on this project skeleton existing. This is the foundation.
Output: A running Next.js 15 app with all toolchain configured and a sample test passing.
</objective>

<execution_context>
@/Users/ahmadali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-determinism-infrastructure/01-RESEARCH.md
@SYNESTHESIA_MACHINE_SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, eslint.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Run create-next-app to scaffold the project in the current directory (it's currently empty except for .planning/ and SYNESTHESIA_MACHINE_SPEC.md):

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
```

If the directory is not empty, you may need to use a temp directory and move files. The key flags:
- --typescript: TypeScript enabled
- --tailwind: Tailwind CSS v4
- --eslint: ESLint configured
- --app: App Router (not Pages)
- --src-dir: Use src/ directory

After scaffold, install Phase 1 dependencies:

```bash
npm install seedrandom next-themes papaparse geist
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/dom vite-tsconfig-paths jsdom @types/seedrandom @types/papaparse
```

Create the project folder structure (empty directories with .gitkeep or index files):
```
src/
  lib/
    engine/          # PRNG, hashing, versioning (Plan 01-02)
    canonicalize/    # Input canonicalization (Plan 01-03)
    cache/           # Cache key generation (Plan 01-02)
  components/
    theme/           # ThemeProvider, ThemeToggle (Plan 01-04)
    layout/          # Header, Shell (Plan 01-04)
  types/
    engine.ts        # VersionInfo, ParameterVector type stubs
```

In `src/types/engine.ts`, create the foundational TypeScript interfaces:

```typescript
export interface VersionInfo {
  engineVersion: string;
  analyzerVersion: string;
  normalizerVersion: string;
  rendererVersion: string;
}

export interface ParameterVector {
  complexity: number;
  warmth: number;
  symmetry: number;
  rhythm: number;
  energy: number;
  density: number;
  scaleVariation: number;
  curvature: number;
  saturation: number;
  contrast: number;
  layering: number;
  directionality: number;
  paletteSize: number;
  texture: number;
  regularity: number;
  extensions?: Record<string, number>;
}

export interface CanonResult {
  canonical: string;
  changes: string[];
  inputType: 'text' | 'json' | 'csv' | 'url';
}

export interface ParameterProvenance {
  parameter: string;
  value: number;
  contributors: Array<{
    signal: string;
    rawValue: number;
    weight: number;
    explanation: string;
  }>;
}
```

Update `src/app/layout.tsx` to use Geist fonts (per user locked decision):

```typescript
import { GeistSans } from 'geist/font/sans';
import { GeistMono } from 'geist/font/mono';
import './globals.css';

export const metadata = {
  title: 'Synesthesia Machine',
  description: 'Turn anything into art',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${GeistSans.variable} ${GeistMono.variable}`} suppressHydrationWarning>
      <body className="font-sans antialiased">
        {children}
      </body>
    </html>
  );
}
```

Update `src/app/globals.css` to configure Tailwind v4 with Geist font variables:

```css
@import "tailwindcss";

@theme {
  --font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif;
  --font-mono: var(--font-geist-mono), ui-monospace, monospace;
}
```

Update `src/app/page.tsx` to a minimal placeholder:

```typescript
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <h1 className="text-4xl font-light tracking-tight">Synesthesia Machine</h1>
    </main>
  );
}
```
  </action>
  <verify>
    <automated>npm run build && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js project scaffolded. `npm run dev` starts the dev server. `npm run build` succeeds. TypeScript compiles. Geist fonts configured in layout.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest and create sample test</name>
  <files>vitest.config.mts, src/__tests__/setup.ts, src/__tests__/sample.test.ts, package.json</files>
  <action>
Create `vitest.config.mts` in project root:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import tsconfigPaths from 'vite-tsconfig-paths';

export default defineConfig({
  plugins: [tsconfigPaths(), react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/__tests__/setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
  },
});
```

Create `src/__tests__/setup.ts` with Web Crypto polyfill for jsdom:

```typescript
// Polyfill crypto.subtle for jsdom environment
// Node.js has full Web Crypto support, but jsdom doesn't expose it
import { webcrypto } from 'node:crypto';

if (typeof globalThis.crypto === 'undefined') {
  Object.defineProperty(globalThis, 'crypto', {
    value: webcrypto,
  });
}
```

Create `src/__tests__/sample.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';

describe('Project setup', () => {
  it('TypeScript is configured', () => {
    const value: string = 'hello';
    expect(value).toBe('hello');
  });

  it('crypto.subtle is available', async () => {
    const encoder = new TextEncoder();
    const data = encoder.encode('test');
    const hash = await crypto.subtle.digest('SHA-256', data);
    expect(hash).toBeInstanceOf(ArrayBuffer);
    expect(hash.byteLength).toBe(32);
  });
});
```

Add test script to `package.json`:

```json
{
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run"
  }
}
```
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest runs successfully. Sample test passes. Web Crypto API is available in tests via setup polyfill. `npm test -- --run` exits with code 0.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- `npm run build` completes successfully
- `npx vitest run` passes all tests
- `npx next lint` runs without config errors
- Geist Sans and Geist Mono fonts render in browser (check Network tab for font files)
- `src/types/engine.ts` exports VersionInfo, ParameterVector, CanonResult, ParameterProvenance
</verification>

<success_criteria>
- Next.js 15 project running with App Router
- TypeScript strict mode compilation succeeds
- Tailwind CSS v4 configured with Geist font variables
- Vitest configured with jsdom + Web Crypto polyfill
- ESLint configured (flat config)
- Geist Sans and Geist Mono fonts loaded via `geist` npm package
- Core type definitions created in src/types/engine.ts
- Project folder structure ready for Plans 01-02 through 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-determinism-infrastructure/01-01-SUMMARY.md`
</output>
