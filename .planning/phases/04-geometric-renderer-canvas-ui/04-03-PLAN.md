---
phase: 04-geometric-renderer-canvas-ui
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/lib/engine/version.ts
  - src/__tests__/engine/version.test.ts
  - src/components/results/ParameterPanel.tsx
autonomous: false
requirements:
  - UI-11
  - UI-12
  - UI-13
  - UI-14
  - UI-15

must_haves:
  truths:
    - "Translation panel displays all 15 parameters as labeled bars with numeric values alongside the rendered canvas"
    - "Parameters are grouped by source (Composition, Form, Expression, Color) with contributing signals and weights"
    - "Each parameter has a plain-English explanation visible to the user"
    - "Engine version is displayed at the bottom of the translation panel"
    - "Panel is expanded on desktop, collapsed on mobile with toggle to expand"
    - "rendererVersion bumped to 0.2.0 reflecting the new geometric renderer"
  artifacts:
    - path: "src/lib/engine/version.ts"
      provides: "Updated CURRENT_VERSION with rendererVersion bumped to 0.2.0"
    - path: "src/components/results/ParameterPanel.tsx"
      provides: "Translation panel with labeled bars, grouping, provenance, and version footer (already built in Phase 3, verified working alongside real canvas)"
  key_links:
    - from: "src/components/results/ResultsView.tsx"
      to: "src/components/results/ParameterPanel.tsx"
      via: "ParameterPanel receives vector, provenance, summaries, version props"
      pattern: "ParameterPanel"
    - from: "src/components/results/ParameterPanel.tsx"
      to: "src/lib/engine/version.ts"
      via: "CURRENT_VERSION displayed in panel footer"
      pattern: "CURRENT_VERSION|version"
---

<objective>
Bump the renderer version to reflect the new geometric renderer, verify the translation panel displays correctly alongside the real canvas, and perform end-to-end human verification of the complete Phase 4 MVP.

Purpose: UI-11 through UI-15 (translation panel requirements) were already implemented in Phase 3. This plan confirms they work correctly in the context of the real rendered canvas, bumps the renderer version, and provides the human verification checkpoint for the complete end-to-end flow.

Output: Updated rendererVersion, verified translation panel alongside canvas, human sign-off on Phase 4 MVP.
</objective>

<execution_context>
@/Users/ahmadali/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-geometric-renderer-canvas-ui/04-CONTEXT.md
@.planning/phases/04-geometric-renderer-canvas-ui/04-01-SUMMARY.md
@.planning/phases/04-geometric-renderer-canvas-ui/04-02-SUMMARY.md

@src/lib/engine/version.ts
@src/components/results/ParameterPanel.tsx
@src/components/results/ResultsView.tsx

<interfaces>
From src/lib/engine/version.ts:
```typescript
export const CURRENT_VERSION: VersionInfo = {
  engineVersion: '0.1.0',
  analyzerVersion: '0.2.0',
  normalizerVersion: '0.3.0',
  rendererVersion: '0.1.0',  // → bump to '0.2.0'
};
```

From src/components/results/ParameterPanel.tsx (already built):
- Accepts: vector (ParameterVector), provenance (ParameterProvenance[]), summaries (Record<string, string>), version (VersionInfo)
- Groups: Composition, Form, Expression, Color
- Each parameter: labeled bar (0-1), numeric value, expandable provenance
- Version footer at bottom
- Collapsible: expanded on desktop, collapsed on mobile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump rendererVersion and verify panel integration</name>
  <files>
    src/lib/engine/version.ts,
    src/__tests__/engine/version.test.ts
  </files>
  <action>
    **Step 1 — Bump rendererVersion:**
    In `src/lib/engine/version.ts`, change `rendererVersion: '0.1.0'` to `rendererVersion: '0.2.0'`. This reflects that the geometric renderer is now shipping. Per CONTEXT.md research: "Bump to '0.2.0' when the geometric renderer is complete. This changes the PRNG seed for any cached results, which is correct — the art has fundamentally changed."

    **Step 2 — Update version test if it exists:**
    Check if `src/__tests__/engine/version.test.ts` has a test that asserts rendererVersion is '0.1.0'. If so, update to '0.2.0'. If no version test exists, add a simple assertion:
    ```
    test('rendererVersion reflects geometric renderer', () => {
      expect(CURRENT_VERSION.rendererVersion).toBe('0.2.0');
    });
    ```

    **Step 3 — Verify ParameterPanel still works:**
    Review src/components/results/ParameterPanel.tsx to confirm it correctly:
    - UI-11: Has collapsible behavior (expanded desktop, collapsed mobile)
    - UI-12: Shows labeled bars (0-1) with numeric values for all 15 parameters
    - UI-13: Groups parameters by source (Composition, Form, Expression, Color) with contributing signals and weights shown on expand
    - UI-14: Displays plain-English explanation per parameter via summaries prop
    - UI-15: Shows engine version at bottom of panel

    If any of these are missing or broken, fix in this task. If all are working (expected — built in Phase 3), no changes needed to ParameterPanel.tsx.

    **Step 4 — Run all tests:**
    `npm run test:run` — verify no regressions from version bump. The PRNG seeds will now be different (rendererVersion is part of seed derivation), so any snapshot-dependent tests may need updating. Check test results carefully.

    `npm run build` — verify build passes.
  </action>
  <verify>
    <automated>npm run test:run && npm run build</automated>
  </verify>
  <done>
    - rendererVersion bumped to '0.2.0' in CURRENT_VERSION
    - Version test passes with new version
    - ParameterPanel verified to satisfy UI-11 through UI-15
    - All tests pass, build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Phase 4 MVP verification</name>
  <files>none (verification only)</files>
  <action>
    This is a human verification checkpoint. No code changes needed.

    What was built:
    - Geometric renderer engine with recursive subdivision and composition laws
    - Canvas display with progressive build animation
    - Style selector with active Geometric and 3 locked future styles
    - Translation panel with 15 parameters, provenance, and engine version
    - All determinism contracts maintained

    Start the dev server: `npm run dev`
    Open http://localhost:3000

    **Flow test:**
    1. Type "Hello, World!" and click Generate
    2. Verify pipeline stages show (Parsing, Analyzing, Normalizing, Rendering)
    3. Verify canvas shows geometric artwork building progressively (elements fade in, large to small)
    4. Verify style selector above canvas shows: Geometric (active, with thumbnail) + Organic, Particle, Typographic (locked, gray, with lock icons)
    5. Verify translation panel beside canvas (desktop) or below (mobile) shows:
       - 15 parameter bars with labels and numeric values
       - Groups: Composition, Form, Expression, Color
       - Expandable provenance per parameter showing signals and weights
       - Plain-English explanation per parameter
       - Engine version at bottom (should show renderer: 0.2.0)

    **Determinism test:**
    6. Type the same text again — verify identical artwork produced
    7. Type different text — verify different artwork

    **Theme test:**
    8. Toggle dark/light mode — verify canvas re-renders instantly with adjusted palette (no animation replay)

    **Accessibility test:**
    9. In browser DevTools, set prefers-reduced-motion to "reduce"
    10. Generate new artwork — verify it appears instantly (no progressive animation)

    **Quick-start test:**
    11. Click "Surprise Me" — verify full flow works end to end with random phrase

    **Mobile test:**
    12. Resize to mobile width — verify canvas goes full-width, parameter panel moves below, style selector remains visible

    **Visual quality:**
    13. Verify artwork looks like geometric composition (rectangles, circles, triangles, lines in grid cells)
    14. Verify "gallery wall" aesthetic — dark background visible between cell gaps
    15. Verify minimum 2% padding from canvas edges
    16. Verify no visual artifacts (sub-pixel lines, overflow, clipping)
  </action>
  <verify>
    <automated>npm run test:run && npm run build</automated>
  </verify>
  <done>
    - Human verifies all 16 checks pass
    - End-to-end MVP flow works: text input through geometric artwork with translation panel
    - Resume signal: "approved" or description of issues
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **UI-11:** Panel collapsible — expanded on desktop, collapsed on mobile with toggle
2. **UI-12:** All 15 parameters shown as labeled bars (0-1) with numeric values
3. **UI-13:** Parameters grouped by source with contributing signals and weights
4. **UI-14:** Plain-English explanation per parameter
5. **UI-15:** Engine version displayed at panel footer (renderer: 0.2.0)
6. **Version:** rendererVersion = '0.2.0' in CURRENT_VERSION
7. **End-to-end:** Text input produces geometric artwork with full parameter transparency
8. **Full suite:** `npm run test:run` all green, `npm run build` succeeds
</verification>

<success_criteria>
- rendererVersion bumped to 0.2.0
- Translation panel (UI-11 through UI-15) confirmed working alongside real canvas
- Human verifies complete end-to-end Phase 4 MVP flow
- Deterministic rendering confirmed (same input = same art)
- Progressive animation and reduced-motion accessibility confirmed
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-geometric-renderer-canvas-ui/04-03-SUMMARY.md`
</output>
