import { describe, it } from 'vitest';
// import { buildOrganicSceneGraph } from '@/lib/render/organic/scene';

describe('organic renderer determinism', () => {
  it.todo('identical inputs produce identical OrganicSceneGraph');
  it.todo('different seeds produce different curve sets');
  it.todo('determinism holds across 3 runs with no hidden state');
});
