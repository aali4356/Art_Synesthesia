import { describe, it, expect } from 'vitest';
// import { buildParticleSceneGraph } from '@/lib/render/particle/scene';

describe('particle renderer determinism', () => {
  it.todo('identical inputs produce identical ParticleSceneGraph');
  it.todo('different seeds produce different particle positions');
  it.todo('determinism holds across 3 runs with no hidden state');
});
