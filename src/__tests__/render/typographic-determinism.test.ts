import { describe, it, expect } from 'vitest';
// import { buildTypographicSceneGraph } from '@/lib/render/typographic/scene';

describe('typographic renderer determinism', () => {
  it.todo('produces identical scene for same inputs (multiple runs)');
  it.todo('produces different scenes for different seeds');
  it.todo('produces different layouts for different input text');
});
