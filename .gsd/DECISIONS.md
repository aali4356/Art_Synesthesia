# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001/S01 | library | Font loading | next/font/google for Geist | Matches project scaffold and avoids extra font-loading complexity | No | agent |
| D002 | M001/S01 | library | PRNG algorithm | Alea via seedrandom | Fast, deterministic, and already integrated across render paths | No | agent |
| D003 | M001/S01 | convention | Randomness policy | Ban Math.random() in render/pipeline code | Determinism is a product promise, not an implementation detail | No | agent |
| D004 | M001/S01 | convention | Theme default | Dark mode default with persisted user choice | Dark backgrounds better frame generated artwork | Yes — if future brand direction changes | agent |
| D005 | M001/S02 | pattern | Normalization strategy | Quantile-based scaling against calibration corpus | Produces more expressive stable distributions than min/max normalization | No | agent |
| D006 | M001/S02 | library | Palette color space | OKLCH via culori | Best fit for perceptual palette control and contrast-safe tuning | No | agent |
| D007 | M001/S03 | pattern | Pipeline orchestration | Module-level cached calibration in client hooks | Avoids recalculating calibration data per render/generation | Yes — if server orchestration changes | agent |
| D008 | M001/S04 | pattern | Renderer architecture | Pure scene-graph builders plus style-specific canvas components | Keeps composition deterministic and testable while isolating Canvas side effects | No | agent |
| D009 | M001/S07 | arch | Cache/database access boundary | Route handlers call boundary modules (`db-cache`, `db-gallery`) instead of inline DB code | Improves isolation, testing, and future refactor safety | No | agent |
| D010 | M001/S09 | pattern | Accessibility/export metadata source | Scene graphs carry source ParameterVector | Enables deterministic alt text and export diagnostics without recomputing pipeline state | No | agent |
| D011 | M002 | scope | Post-M001 sequencing | Use a design-first multi-milestone sequence (M002 art engine, M003 full-site redesign, M004 product coherence, M005 launch hardening) | Matches user intent to expand ambition rather than shrink to an MVP cleanup | Yes — if scope changes materially | agent |
| D012 | M002 | design | Core aesthetic direction | Editorial gallery luxe | User wants super artsy, sleek, premium presentation rather than neutral SaaS polish | Yes — if user changes art direction | agent |
| D013 | M002 | design | Palette strategy | Wide curated deterministic palette families | Directly addresses the user complaint that current outputs feel stuck in repetitive purple/orange/green bands | No | agent |
| D014 | M002 | scope | Milestone focus | M002 prioritizes palette + renderer expressiveness before full-site redesign | Better artwork quality should precede broader visual shell redesign so later polish showcases stronger output | No | agent |
| D015 | M002 | product | Product identity | Treat Synesthesia Machine as a distinct branded art product, not just a nicer portfolio demo | User wants a full product and public launch readiness, not only cosmetic cleanup | Yes — if launch strategy changes | agent |
| D016 | M005 | operability | Launch target | Optimize for public portfolio launch | Balances ambition with realistic launch scope; does not require full SaaS commercialization yet | Yes — if user later pursues commercial productization | agent |
| D017 | M002/S01 | verification | Palette-family proof strategy | Add explicit family-selection and family-diversity tests before implementation | S01 must prove reduced repetition through executable contract tests rather than subjective review or metadata-only assertions | No | agent |
| D018 | M002/S01 | pattern | Palette generator architecture | Split `generatePalette()` into deterministic family selection plus family-specific realization while preserving the existing shared call shape | Keeps text/URL/data integration stable while creating the boundary S02 needs for mood-driven mapping upgrades | No | agent |
| D019 | M002/S01 | observability | Palette diagnostics surface | Expose stable family metadata on `PaletteResult` for inspection and downstream consumption | Gives future agents and later slices direct visibility into palette-family outcomes without reproducing hidden branch logic | Yes — if a better shared diagnostic surface emerges | agent |
| D020 | M002/S01 | pattern | Palette family realization | Store family-specific hue/chroma/lightness shaping coefficients in `palette-families.ts` and drive realization from data rather than branching logic | Keeps curated family behavior inspectable and tunable while preserving the shared `generatePalette()` seam | Yes — if later slices need a richer aesthetic model | agent |
| D021 | M002/S01 | verification | Slice gate policy | Do not mark S01 complete unless family-selection, family-diversity, and baseline palette tests pass together | Prevents metadata-only progress from being mistaken for a safe palette-family system | No | agent |
| D022 | M002/S02 | pattern | Synesthetic mapping architecture | Add a first-class deterministic synesthetic mapping artifact consumed by `generatePalette()` while preserving the shared `generatePalette(vector, seed)` seam | Gives S02 a real full-vector aesthetic-intent boundary and creates the downstream contract S03 can consume without changing hook entrypoints | No | agent |
| D023 | M002/S02 | observability | Synesthetic diagnostics surface | Expose additive mapping diagnostics on `PaletteResult` and prove them through hook integration tests | Makes mood/family/harmony intent inspectable at the shared palette seam and across text/URL/data flows instead of hiding behavior in implementation details | Yes — if a better cross-slice diagnostics surface emerges | agent |
| D024 | M002/S02 | pattern | Hook determinism proof policy | Treat hook-level determinism as canonical-input stability of vector and mapping diagnostics rather than strict full-palette equality when seed derivation is not canonicalized | Keeps integration tests aligned with the real runtime contract and localizes the remaining determinism gap to seed derivation instead of writing false guarantees | Yes — if later work canonicalizes data-path seed derivation | agent |
| D025 | M002/S03 | pattern | Renderer expressiveness architecture | Introduce one shared pure renderer-expression interpretation seam from `PaletteResult.mapping`, then have organic and typographic scene builders consume it instead of re-deriving mood from raw params | Preserves cross-style coherence, keeps scene builders deterministic/testable, and makes mapping-driven art direction inspectable at the scene-graph boundary | Yes — if later slices require a richer shared art-direction contract | agent |
| D026 | M002/S03 | verification | Renderer expressiveness proof strategy | Use mapping-aware renderer scene tests plus existing ResultsView/StyleSelector and hook integration suites as the slice gate before browser-level art proof | Proves two styles visibly benefit from the S02 mapping seam while staying honest that S04 still owns live browser acceptance | No | agent |
| D027 | M002/S03 | observability | Renderer diagnostics boundary | Expose applied expressiveness posture directly on organic and typographic scene graphs | Keeps mapping-driven art direction inspectable at the runtime scene boundary and lets future slices localize issues without reproducing hidden renderer logic | Yes — if a better cross-renderer inspection surface emerges | agent |
| D028 | M002/S04 | verification | Live proof strategy | Define S04 completion with failing acceptance tests, restored `npm run build`, and browser-level assertions/artifacts across text, URL, and data flows | Keeps milestone closeout tied to executable and browser-visible proof instead of subjective screenshots or test-only claims | No | agent |
| D029 | M002/S04 | observability | Live acceptance diagnostics boundary | Expose concise derived family/mapping/expressiveness diagnostics in the real results surface without revealing raw inputs | Gives future agents a trustworthy first inspection seam for weak live outputs while preserving privacy boundaries in durable proof artifacts | Yes — if a better launch-facing diagnostics surface emerges | agent |
| D030 | M002/S04 | operability | Build-time database boundary | Defer DB-backed route/gallery imports behind runtime guards so `next build` can complete without `DATABASE_URL` in local proof mode | Keeps slice verification focused on the art-proof/runtime seam while preserving explicit 503 failure visibility for DB-backed routes when the database is intentionally unavailable | Yes — if local build environments always provide database credentials | agent |
| D031 | M002/S04 | verification | Live closeout truth boundary | Treat S04 as complete once text/data browser proof and build health pass and the remaining URL runtime dependency is explicitly surfaced with actionable diagnostics | Keeps milestone closeout honest without hiding real blockers, while avoiding a false binary where integrated proof is discarded despite a trustworthy failure surface | Yes — if URL analysis becomes fully DB-optional later | agent |
| D032 | M003 | planning | Slice ordering strategy | Plan M003 around the existing `page.tsx` landing/results seam first, then shared shell/action/viewer primitives, then final gallery/compare/share/export integration | The hardest milestone risk is whether one editorial direction survives the real landing-to-results journey; proving that first creates a stable branded language downstream routes can consume instead of fragmenting into page-by-page redesigns | Yes — if implementation reveals a different cross-surface dependency order | agent |
| D033 | M003 | scope | Requirement boundary | Treat R003, R004, and R009 as the core M003 contract, with R005 and R010 partially advanced but not fully closed in this milestone | Keeps M003 focused on premium product experience and coherent browser-visible redesign while preserving launch hardening and full accessibility breadth for their existing later owners | Yes — if milestone scope is renegotiated | agent |
| D034 | M003/S01 | verification | Slice proof strategy | Define S01 completion with a failing-then-passing homepage editorial flow test, preserved ResultsView live-proof contract, passing local build, and real browser verification including honest URL no-DB failure-path inspection | Keeps the highest-risk landing/results seam tied to executable and runtime truth instead of subjective design review alone | No | agent |
| D035 | M003/S01 | observability | Live inspection seam | Treat homepage progress state, ResultsView proof diagnostics, and visible action/error states as the canonical first inspection surfaces for the redesigned landing/results journey | Gives future agents a stable browser-visible path to localize generation and continuity problems without exposing raw input | Yes — if a stronger shared diagnostics surface is introduced later | agent |
| D036 | M003/S01/T02 | design-system | Landing redesign token strategy | Express the homepage redesign through shared editorial shell/panel/support/action utilities in `globals.css` instead of page-local classes alone | Keeps the branded intake surface reusable for downstream results/shell work and avoids another isolated one-off hero treatment | Yes — if a broader design-token layer supersedes these utilities later | agent |
| D037 | M003/S01/T03 | design-system | Results action/modal token strategy | Extend the same editorial token layer to results-only cards, chips, modal shells, and form fields rather than relying on undefined semantic utility classes | Preserves landing→results continuity, removes styling drift in export/share/save surfaces, and gives future slices reusable branded primitives for adjacent routes | Yes — if a more formal design-token system replaces these utilities later | agent |
| D038 | M003/S02 | verification | Shared-surface proof strategy | Define S02 completion with a failing-then-passing shared route-surface test, preserved compare/gallery/share contracts, passing build, and browser verification across browse/compare/detail routes including truthful unavailable-state inspection | Keeps cross-surface coherence tied to executable and runtime truth rather than subjective visual review, and ensures failure-path messaging stays visible while the design system propagates | No | agent |
| D039 | M003/S02 | pattern | Shell/viewer normalization strategy | Normalize gallery browse, compare, gallery detail, and share detail onto shared editorial shell, route-intro, and viewer scaffolds built from the S01 token layer instead of page-local wrappers or a second design system | Directly addresses the route-fragmentation risk identified in research, removes undefined compare utilities, and creates reusable branded seams S03 can finish integrating | Yes — if a later formal component system supersedes these primitives | agent |
| D040 | M003/S03 | verification | Final family-assembly proof strategy | Define S03 completion with one assembled route-family test, preserved export/share/gallery contracts, passing build, and real browser verification from homepage results into gallery/compare/share-detail or truthful unavailable states | Keeps R004 tied to executable and browser-visible family continuity instead of isolated page polish, while explicitly rechecking keyboard/accessibility-aware behavior and DB-truthful diagnostics | No | agent |
| D041 | M003/S03/T01 | copy-contract | Action-family continuity language | Reuse the same collector/editorial phrases across results actions and downstream compare/share/gallery viewers while keeping each route’s truth boundary explicit | Closes the product-family gap without collapsing distinct contracts: share remains parameter-only, gallery hints remain optional, and export copy stays honest about current backend limits | Yes — if later brand language evolves | agent |
| D042 | M004 planning | observability | Analytics and error-monitoring stack for M004 | Use privacy-filtered PostHog for product analytics and Sentry for error monitoring/tracing, with raw source text, raw URLs, and optional hint content excluded by default. | M004 needs real launch-grade usage and failure visibility, but the product’s private-first posture makes capture discipline non-negotiable. PostHog fits the App Router analytics/provider model with masking controls, while Sentry provides the clearest Next.js App Router error and trace path without requiring a heavier backoffice. | Yes | agent |
| D043 | M004 planning | persistence | Restoration contract for returning work | Treat M004 continuity as edition-family recall, not a promise of exact pixel-identical session restoration, unless a later explicit seed-persistence contract is added. | Current share/gallery viewers rebuild from synthetic seeds and do not prove exact restoration. Making the continuity contract explicit avoids misleading copy, unnecessary schema overreach, and late redesign when the product can already deliver useful repeat-use behavior through truthful recall. | Yes | agent |
| D044 | M004 planning | observability | Analytics and error-monitoring stack for M004 | Use privacy-filtered PostHog for product analytics and Sentry for error monitoring/tracing, with raw source text, raw URLs, and optional hint content excluded by default. | M004 needs real launch-grade usage and failure visibility, but the product’s private-first posture makes capture discipline non-negotiable. PostHog fits the App Router analytics/provider model with masking controls, while Sentry provides the clearest Next.js App Router error and trace path without requiring a heavier backoffice. | Yes | agent |
| D045 | M004/S01 planning | persistence | Anonymous-first continuity storage shape for returning users | Use a browser-local recent-work store for M004/S01, capped to a small ordered list and containing only parameter-safe edition-family metadata plus preferred style/freshness labels; expose it as 'recent local work' distinct from public share links and gallery saves. | This satisfies R008 without introducing accounts, stays consistent with D043's edition-family recall contract, preserves the private-first posture by excluding raw source content, and gives returning users an intentional reopen seam before any heavier identity/persistence system exists. | Yes | agent |
| D046 | M004 S01 T01 | architecture | Anonymous recent-work browser storage contract for continuity seam | Store only a privacy-safe edition snapshot (parameter vector, palette snapshot, preferred style, savedAt/lastOpenedAt, and derived source labels such as hostname-only or dataset shape) behind a single continuity helper module; do not store raw text, full URLs, dataset bodies, or exact-session replay state. | Later UI work needs enough local metadata to intentionally reopen a recent edition family from the homepage, but storing replay-grade source material would violate the slice privacy boundary and over-promise exact restoration. The palette snapshot keeps reopen behavior usable without depending on raw canonical input, while the explicit posture keeps continuity scoped to same-browser edition-family recall. | Yes | agent |
| D047 | M004 S01 T02 | continuity | How resumed recent local work reconstructs the results surface without stored raw source | Resume saved local work from the stored parameter vector, palette snapshot, source labels, and preferred style only; synthesize a continuity-safe canonical seed and placeholder source text instead of replaying original text, URL, or dataset bodies. | The continuity contract from T01 forbids persisting raw source content, but T02 still needs a usable reopened results surface. Reconstructing scenes from parameter-safe snapshots preserves privacy, keeps reopening functional in the same browser, and stays truthful that local continuity is edition-family recall rather than full session replay. | Yes | agent |
| D048 | M004/S01 | copy-contract | M004/S01 local continuity vs public persistence language | Keep recent local work discoverable from the shared header/homepage seam and enforce explicit browser-local/private-first wording distinct from public Share links and Gallery saves. | T03 verified that returning users can rediscover recent local work from the shell and understand, from copy alone, that local continuity is same-browser private recall while share/gallery remain public routes. Cross-surface coherence tests now lock this distinction across header, continuity panel, share action, and gallery modal. | Yes | agent |
| D049 | M004/S01 | requirement | R008 | validated | M004/S01 now ships an intentional anonymous-first continuity layer: users can save an edition family into browser-local recent work, return to the homepage in the same browser, and intentionally reopen it without persisting raw source text, full URLs, or dataset bodies. Slice-level tests and live browser verification passed for storage privacy, save/reload/resume flow, and local-vs-public continuity copy boundaries. | Yes | agent |
| D050 | M004/S02 planning | architecture | S02 onboarding and route-discovery composition seam | Drive first-visit, returning, and resumed onboarding from existing homepage continuity state (`recentWorksLoaded`, `recentWorks.length`, and `resumedWork`) and add a shared header navigation landmark with active-route semantics instead of introducing new persisted onboarding flags or another continuity route. | S01 already established the privacy-safe recent-work seam, so S02 can satisfy adaptive guidance and repeat-use navigation in the product layer without reopening storage contracts. Keeping visitor mode as derived UI state follows React best practices, preserves the truthful browser-local/private distinction from D045-D048, and gives home, compare, and gallery surfaces one coherent navigation family for R004. | Yes | agent |
| D051 | M004/S02/T01 | architecture | How adaptive onboarding state is determined for the homepage continuity experience | Homepage onboarding now derives first-visit vs returning vs resumed guidance from recent-local-work load state and resumed work presence. | This preserves the existing privacy-safe continuity seam, avoids introducing new persisted onboarding flags or storage shape, and keeps first-time versus returning guidance truthful to browser-local state. | Yes | agent |
| D052 | M004 S02 T03 | pattern | How results surfaces distinguish fresh results from reopened recent-local results | Pass an explicit continuityMode prop from Home into ResultsView and render route-next-step guidance from that state. | Resume-versus-fresh guidance is part of the cross-surface product contract. Driving it from Home's existing continuity state keeps the results copy truthful, avoids brittle inference from input labels or storage text, and preserves the browser-local versus public-route boundary in one place. | Yes | agent |
| D053 | M004/S02 | architecture | Shared-shell active-route state for adaptive onboarding/navigation surfaces | Drive header active-route semantics from an explicit Shell route-family prop instead of deriving pathname state inside Header. | M004/S02 needed one truthful navigation family across Home, Compare, and Gallery without route-local drift. Passing route family explicitly kept aria-current stable, preserved the browser-local versus public-route wording boundary, and avoided coupling Header to pathname inference. | Yes | agent |
| D054 | M004/S03 planning | accessibility | How S03 should repair keyboard semantics across custom selection widgets and modal flows | Prefer native button-based controls with explicit selected/pressed state, add a real skip-link/main-focus target in the shared shell, and implement full focus lifecycle management for the gallery modal instead of relying on ARIA-decorated generic containers alone. | The current gaps are concentrated in custom widgets (`StyleSelector`, `InputTabs`, and `GallerySaveModal`) where partial ARIA semantics are already drifting from actual behavior. Native controls plus explicit focus management reduce keyboard regressions, align with the installed accessibility skill, and make the product-family surfaces more robust for launch readiness. | Yes | agent |
| D055 | M004 S03 T02 | observability | How repeat-use client telemetry should distinguish homepage continuity state from storage-side actions | Pass continuity metadata from the homepage/results seams into useRecentWorks so one storage hook emits continuity events while results emits only style/save-intent interactions | This keeps continuity events single-sourced, preserves fresh-vs-resumed context without duplicate captures, and avoids page-level telemetry drift while maintaining the private-first redaction contract. | Yes | agent |
| D056 | M004/S03/T03 | observability | How server-side route events should be emitted when only Sentry is configured | Emit sanitized server observability events through the shared server adapter as Sentry info messages, while route failures continue to use exception capture | This makes route.unavailable and similar server-only product signals inspectable in configured environments without inventing a second server analytics adapter, while preserving the existing sanitized exception path for true failures. | Yes | agent |
| D057 | M004/S03 | requirement | R010 | validated | M004/S03 completed skip-link/main-target navigation, truthful tab semantics and keyboard style switching, gallery modal initial focus/trap/escape restoration, reduced-motion coverage, and passing regression plus live browser proof across Home → Results → Gallery/Share surfaces. | Yes | agent |
| D058 | M004/S03 | requirement | R010 | validated | M004/S03 completed skip-link navigation, keyboard-operable selection controls, modal focus trapping/restoration, reduced-motion coverage, and passing regression plus live browser proof across the redesigned repeat-use surfaces. | Yes | agent |
| D059 | M004/S03 closeout | requirement | R007 | validated | M004/S03 added privacy-filtered product-loop/public-route observability, categorized server failure capture, and live/browser-tested truthful unavailable-state diagnostics without exposing raw source content. | Yes | agent |
