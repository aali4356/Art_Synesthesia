# Decisions Register

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001/S01 | library | Font loading | next/font/google for Geist | Matches project scaffold and avoids extra font-loading complexity | No |
| D002 | M001/S01 | library | PRNG algorithm | Alea via seedrandom | Fast, deterministic, and already integrated across render paths | No |
| D003 | M001/S01 | convention | Randomness policy | Ban Math.random() in render/pipeline code | Determinism is a product promise, not an implementation detail | No |
| D004 | M001/S01 | convention | Theme default | Dark mode default with persisted user choice | Dark backgrounds better frame generated artwork | Yes — if future brand direction changes |
| D005 | M001/S02 | pattern | Normalization strategy | Quantile-based scaling against calibration corpus | Produces more expressive stable distributions than min/max normalization | No |
| D006 | M001/S02 | library | Palette color space | OKLCH via culori | Best fit for perceptual palette control and contrast-safe tuning | No |
| D007 | M001/S03 | pattern | Pipeline orchestration | Module-level cached calibration in client hooks | Avoids recalculating calibration data per render/generation | Yes — if server orchestration changes |
| D008 | M001/S04 | pattern | Renderer architecture | Pure scene-graph builders plus style-specific canvas components | Keeps composition deterministic and testable while isolating Canvas side effects | No |
| D009 | M001/S07 | arch | Cache/database access boundary | Route handlers call boundary modules (`db-cache`, `db-gallery`) instead of inline DB code | Improves isolation, testing, and future refactor safety | No |
| D010 | M001/S09 | pattern | Accessibility/export metadata source | Scene graphs carry source ParameterVector | Enables deterministic alt text and export diagnostics without recomputing pipeline state | No |
| D011 | M002 | scope | Post-M001 sequencing | Use a design-first multi-milestone sequence (M002 art engine, M003 full-site redesign, M004 product coherence, M005 launch hardening) | Matches user intent to expand ambition rather than shrink to an MVP cleanup | Yes — if scope changes materially |
| D012 | M002 | design | Core aesthetic direction | Editorial gallery luxe | User wants super artsy, sleek, premium presentation rather than neutral SaaS polish | Yes — if user changes art direction |
| D013 | M002 | design | Palette strategy | Wide curated deterministic palette families | Directly addresses the user complaint that current outputs feel stuck in repetitive purple/orange/green bands | No |
| D014 | M002 | scope | Milestone focus | M002 prioritizes palette + renderer expressiveness before full-site redesign | Better artwork quality should precede broader visual shell redesign so later polish showcases stronger output | No |
| D015 | M002 | product | Product identity | Treat Synesthesia Machine as a distinct branded art product, not just a nicer portfolio demo | User wants a full product and public launch readiness, not only cosmetic cleanup | Yes — if launch strategy changes |
| D016 | M005 | operability | Launch target | Optimize for public portfolio launch | Balances ambition with realistic launch scope; does not require full SaaS commercialization yet | Yes — if user later pursues commercial productization |
| D017 | M002/S01 | verification | Palette-family proof strategy | Add explicit family-selection and family-diversity tests before implementation | S01 must prove reduced repetition through executable contract tests rather than subjective review or metadata-only assertions | No |
| D018 | M002/S01 | pattern | Palette generator architecture | Split `generatePalette()` into deterministic family selection plus family-specific realization while preserving the existing shared call shape | Keeps text/URL/data integration stable while creating the boundary S02 needs for mood-driven mapping upgrades | No |
| D019 | M002/S01 | observability | Palette diagnostics surface | Expose stable family metadata on `PaletteResult` for inspection and downstream consumption | Gives future agents and later slices direct visibility into palette-family outcomes without reproducing hidden branch logic | Yes — if a better shared diagnostic surface emerges |
| D020 | M002/S01 | pattern | Palette family realization | Store family-specific hue/chroma/lightness shaping coefficients in `palette-families.ts` and drive realization from data rather than branching logic | Keeps curated family behavior inspectable and tunable while preserving the shared `generatePalette()` seam | Yes — if later slices need a richer aesthetic model |
| D021 | M002/S01 | verification | Slice gate policy | Do not mark S01 complete unless family-selection, family-diversity, and baseline palette tests pass together | Prevents metadata-only progress from being mistaken for a safe palette-family system | No |
| D022 | M002/S02 | pattern | Synesthetic mapping architecture | Add a first-class deterministic synesthetic mapping artifact consumed by `generatePalette()` while preserving the shared `generatePalette(vector, seed)` seam | Gives S02 a real full-vector aesthetic-intent boundary and creates the downstream contract S03 can consume without changing hook entrypoints | No |
| D023 | M002/S02 | observability | Synesthetic diagnostics surface | Expose additive mapping diagnostics on `PaletteResult` and prove them through hook integration tests | Makes mood/family/harmony intent inspectable at the shared palette seam and across text/URL/data flows instead of hiding behavior in implementation details | Yes — if a better cross-slice diagnostics surface emerges |
| D024 | M002/S02 | pattern | Hook determinism proof policy | Treat hook-level determinism as canonical-input stability of vector and mapping diagnostics rather than strict full-palette equality when seed derivation is not canonicalized | Keeps integration tests aligned with the real runtime contract and localizes the remaining determinism gap to seed derivation instead of writing false guarantees | Yes — if later work canonicalizes data-path seed derivation |
| D025 | M002/S03 | pattern | Renderer expressiveness architecture | Introduce one shared pure renderer-expression interpretation seam from `PaletteResult.mapping`, then have organic and typographic scene builders consume it instead of re-deriving mood from raw params | Preserves cross-style coherence, keeps scene builders deterministic/testable, and makes mapping-driven art direction inspectable at the scene-graph boundary | Yes — if later slices require a richer shared art-direction contract |
| D026 | M002/S03 | verification | Renderer expressiveness proof strategy | Use mapping-aware renderer scene tests plus existing ResultsView/StyleSelector and hook integration suites as the slice gate before browser-level art proof | Proves two styles visibly benefit from the S02 mapping seam while staying honest that S04 still owns live browser acceptance | No |
| D027 | M002/S03 | observability | Renderer diagnostics boundary | Expose applied expressiveness posture directly on organic and typographic scene graphs | Keeps mapping-driven art direction inspectable at the runtime scene boundary and lets future slices localize issues without reproducing hidden renderer logic | Yes — if a better cross-renderer inspection surface emerges |
